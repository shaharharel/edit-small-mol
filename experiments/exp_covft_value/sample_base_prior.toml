# Exp 1 — sample 10k from BASE prior (no covalent FT), Mol1 anchor
run_type = "sampling"
json_out_config = "_sample_base.json"

[parameters]
model_file = "/Users/shaharharel/Documents/github/REINVENT4/priors/mol2mol_medium_similarity.prior"
smiles_file = "mol1_only.smi"
sample_strategy = "multinomial"
temperature = 1.0
output_file = "samples_base.csv"
num_smiles = 10000
unique_molecules = false
randomize_smiles = true
